version: 2

jobs:
  linux:
    docker:
      - image: vapor/swift:5.0
      - image: vimagick/tinyproxy
        name: tinyproxy
    steps:
      - checkout
      - run: swift build -Xswiftc -DDEBUG
      - run: swift test -Xswiftc -DDEBUG

workflows:
  version: 2
  tests:
    jobs:
      - linux
